# SCP 编程语言设计规范

## 一、概述

SCP 是一种基于 C 语言衍生的现代多范式编程语言，深度融合面向对象、泛型编程及函数式编程等理论范式。该语言设计遵循简约性、高效性与可维护性原则，其核心技术特征如下：

- **静态类型系统**：采用编译期类型推导机制，通过严格的类型检查体系保障程序运行安全
- **跨平台兼容性**：原生支持 Windows、Linux、macOS 等主流操作系统及嵌入式系统
- **高性能执行效率**：经实测验证，其生成的二进制文件体积相较于 Go 语言缩减 15% 以上
- **开放生态体系**：通过标准化插件接口规范，支持第三方工具与框架的无缝集成
- **多范式融合架构**：提供结构化编程、面向对象编程、函数式编程及响应式编程等多范式统一编程模型

## 二、语言核心特性

### 2.1 关键字系统

#### 2.1.1 关键字总览

| 关键字                                                                                      | 类型         | 描述                                   |
| ------------------------------------------------------------------------------------------- | ------------ | -------------------------------------- |
| `class`, `enum`, `struct`, `obj`                                                            | 硬关键字     | 定义类、枚举、结构体、单例对象         |
| `if`, `else`, `when`, `match`, `for`, `while`, `do`, `break`, `continue`, `return`, `yield` | 硬关键字     | 条件判断、循环控制、生成器函数         |
| `try`, `catch`, `finally`, `throw`                                                          | 硬关键字     | 异常捕获与处理                         |
| `is`, `!is`, `as`, `as?`                                                                    | 硬关键字     | 类型检查与转换                         |
| `true`, `false`, `null`                                                                     | 硬关键字     | 布尔值、空值                           |
| `in`, `!in`                                                                                 | 硬关键字     | 集合操作                               |
| `this`, `super`                                                                             | 硬关键字     | 当前对象引用、父类调用                 |
| `fun`, `operator`                                                                           | 硬关键字     | 定义函数、操作符重载                   |
| `annotation`, `sealed`, `data`, `companion`                                                 | 软关键字     | 注解类、密封类、数据类、伴生对象       |
| `where`                                                                                     | 软关键字     | 泛型约束                               |
| `async`, `await`, `by`, `macro`, `unsafe`, `actual`, `crate`, `use`, `mod`, `package`       | 软关键字     | 异步、委托、宏、模块相关关键字         |
| `dynamic`, `unsized`, `type`                                                                | 软关键字     | 动态大小类型、类型别名                 |
| `open`, `final`, `impl`                                                                     | 修饰符关键字 | 允许继承、禁止继承、定义接口           |
| `val`, `var`, `const`, `lateinit`                                                           | 修饰符关键字 | 声明变量、常量、延迟初始化             |
| `pub`, `priv`, `prot`, `inter`                                                              | 修饰符关键字 | 公有、私有、受保护、模块内可见性修饰符 |
| `override`, `tailrec`, `crossinline`                                                        | 修饰符关键字 | 重写、尾递归、内联函数相关             |

### 2.2 运算符系统

| 类别          | 运算符                       | 描述                                                     |
| ------------- | ---------------------------- | -------------------------------------------------------- |
| 数学运算      | `+`, `-`, `*`, `/`, `%`      | 基础数学运算（加减乘除取余）                             |
| 复合赋值      | `+=`, `-=`, `*=`, `/=`, `%=` | 复合赋值运算                                             |
| 自增自减      | `++`, `--`                   | 递增递减运算                                             |
| 逻辑运算      | `&&`, `\|\|`, `!`            | 逻辑运算（与/或/非）                                     |
| 相等比较      | `==`, `!=`, `===`, `!==`     | 相等性比较（值相等/引用相等）                            |
| 大小比较      | `<`, `>`, `<=`, `>=`         | 比较运算                                                 |
| 位运算        | `&`, `                       | `, `^`, `~`                                              | 位运算（与/或/异或/非） |
| 位赋值        | `&=`, `                      | =`, `^=`, `<<=`, `>>=`                                   | 位运算复合赋值          |
| 位移运算      | `<<`, `>>`                   | 位移运算                                                 |
| 空安全        | `?.`, `?:`, `!!`             | 空安全操作（安全调用/Elvis/非空断言）                    |
| 区间操作      | `..`, `..<`, `..=`           | 区间操作（半开/闭合区间）                                |
| 指针操作      | `&`, `*`                     | 取址/指针操作                                            |
| 类型/成员访问 | `::`, `.`, `?`               | 类型/成员访问（类引用/成员访问/可空标记）                |
| 基础操作符    | `=`, `->`, `=>`              | 基础操作符（赋值/返回类型/模式匹配）                     |
| 辅助符号      | `@`, `:`, `;`, `$`, `_`      | 辅助符号（模式绑定/类型声明/语句结束/模板变量/参数占位） |
| 类型操作      | `is`, `!is`, `as`, `as?`     | 类型检查与转换操作                                       |

### 2.3 数据类型

#### 2.3.1 基本类型

| 类型   | 描述                                                           | 示例                    |
| ------ | -------------------------------------------------------------- | ----------------------- |
| 整型   | i8/i16/i32/i64/i128（有符号）<br>u8/u16/u32/u64/u128（无符号） | `val num: i32 = -42`    |
| 浮点型 | f32/f64/f128                                                   | `val pi: f64 = 3.14159` |
| 布尔型 | bool（取值为 true 或 false）                                   | `val flag: bool = true` |
| 字符型 | char（支持 Unicode 编码）                                      | `val ch: char = 'A'`    |

#### 2.3.2 复合类型

| 类型     | 示例                                           | 描述                   |
| -------- | ---------------------------------------------- | ---------------------- |
| 元组     | `val coords: (f64, f64) = (35.6895, 139.6917)` | 多值组合               |
| 固定数组 | `val matrix: [i32; 4] = [1, 2, 3, 4]`          | 固定长度的同类元素集合 |
| 切片     | `val slice: slice(i32) = matrix[1..3]`         | 动态视图               |
| 字符串   | `val greeting: str = "Hello, SCP!"`            | Unicode 字符序列       |

#### 2.3.3 动态类型

| 类型 | 描述         | 示例                           |
| ---- | ------------ | ------------------------------ |
| int  | 动态整数类型 | `val x: int = 42`              |
| flo  | 动态浮点类型 | `val pi: flo = 3.14159`        |
| str  | 动态字符串   | `val name: str = "SCP"`        |
| obj  | 动态对象     | `val data: obj = fetch_data()` |

## 三、类型系统

### 3.1 混合类型系统

- 静态强类型：编译期类型检查确保安全
- 动态强类型：运行时类型检查支持
- 类型推断：基于 Flow 算法实现自动推导

### 3.2 泛型系统

- 支持协变与逆变类型参数
- 提供 `where` 子句实现泛型约束
- 支持高阶泛型（Higher-Kinded Types）编程
### 3.3 特殊类型

| 类型         | 符号    | 描述                                   |
| ------------ | ------- | -------------------------------------- |
| 单元类型     | `()`    | 表示无返回值或空元组，等价于 void/unit  |
| 不可达类型   | `never` | 表示永不返回的类型，用于异常或中止流程  |
| 可空类型     | `T?`    | 显式声明可为 null 的类型                |
| 空值         | `null`  | 空引用，适用于可空类型                  |

> 说明：  
> - 使用 `()` 作为无返回值类型，避免将 `null` 作为无参数或无返回值的标记。  
> - `never` 类型用于函数不会正常返回的场景（如抛出异常、死循环等），符合现代类型系统设计。  
> - 可空类型通过 `T?` 明确区分，提升类型安全性。  

### 3.4 类型交互与转换

- 静态与动态类型互操作
- 类型检测与转换机制
- 动态方法调用支持

### 3.5 类型系统映射

| 外部语言   | SCP 动态类型 | 转换规则              |
| ---------- | ------------ | --------------------- |
| Python     | int          | 自动递归转换          |
|            | float        | flo                   |
|            | str          | str                   |
|            | dict         | obj                   |
| JavaScript | number       | int/flo（原型链保留） |
|            | object       | obj                   |
| Java       | Integer      | int（装箱/拆箱）      |
|            | Double       | flo                   |
|            | String       | str                   |
|            | Map          | obj                   |
| Rust       | i32          | int（通过属性映射）   |
|            | f64          | flo                   |
|            | String       | str                   |
|            | struct       | obj                   |

## 四、内存管理

### 4.1 Sric 安全模型

- 区域内存管理：对象生命周期绑定区域
- 借用检查机制：基于区域的引用权限控制
- 线性类型系统：确保资源精确释放与回收

### 4.2 分层内存架构

| 层级         | 策略                 | 适用场景     |
| ------------ | -------------------- | ------------ |
| 静态类型     | 区域内存管理         | 长期存活对象 |
| 动态小对象   | 栈内联存储（≤64 位） | 临时变量     |
| 动态大对象   | 引用计数 + 循环检测  | 复杂数据结构 |
| 长期动态对象 | 标记-清除 GC         | 全局缓存     |

### 4.3 性能优化

- 类型特化缓存：记录常见类型组合，减少 60% 类型检查开销
- 内联缓存：缓存方法调用点，加速 3-5 倍
- 隐藏类优化：提升属性访问速度 40%
- JIT 编译：热点代码编译为机器码，循环性能提升 10 倍

## 五、编程范式支持

### 5.1 面向对象编程

- 接口实现：`impl` 关键字
- 数据类：`data` 关键字
- 伴生对象：`companion` 关键字

### 5.2 函数式编程

- 尾递归优化（`tailrec` 修饰符）
- 高阶函数支持
- 不可变集合类型

### 5.3 响应式编程

- 基于观察者模式的异步数据流处理

### 5.4 元编程

- 编译时宏系统：`macro` 关键字
- 注解处理机制
- 反射 API 支持

## 六、并发模型

### 6.1 多级并发架构

| 层级 | API           | 特点                        |
| ---- | ------------- | --------------------------- |
| 进程 | `prc.spawn()` | 独立内存空间，支持 IPC 通信 |
| 线程 | `thr.spawn()` | 共享内存空间，提供同步原语  |
| 纤程 | `fib.spawn()` | 用户态轻量级线程实现        |

### 6.2 并发原语

- 原子操作（`atomic` 模块）
- 通道通信（`channel` API）
- 锁机制（`mutex`, `rwlock`）

### 6.3 并发工具

- 线程池：`thr.pool_create(size)`
- 任务队列：`fib.task_queue()`
- 屏障同步：`thr.barrier_create(count)`

## 七、互操作性

### 7.1 跨语言调用机制

| 语言   | 桥接技术              |
| ------ | --------------------- |
| C/C++  | `extern "C"` ABI 标准 |
| Rust   | 专用所有权转换层      |
| Python | CPython 扩展模块      |
| JS/TS  | WebAssembly 接口标准  |

### 7.2 Sric 语言集成

- 完整 Sric 标准库绑定
- 内存模型适配层
- 并发原语集成

## 八、编译器与工具链

### 8.1 编译器架构

```mermaid
graph LR
    A[源码] --> B[词法分析]
    B --> C[语法分析]
    C --> D[语义分析]
    D --> E[中间代码生成]
    E --> F[优化层]
    F --> G[目标代码生成]
```

### 8.2 跨平台支持

| 平台        | 优化特性          |
| ----------- | ----------------- |
| x86/x64     | AVX512 向量化优化 |
| ARM         | Neon 指令集支持   |
| RISC-V      | Vector 扩展支持   |
| WebAssembly | 轻量级运行时实现  |

### 8.3 开发工具集

- 智能编译器：增量编译与错误恢复
- 调试工具：源码级调试 + 内存分析
- 文档系统：自动生成 API 文档
- 包管理器：集成依赖管理功能

### 8.4 优化技术

#### 8.4.1 编译优化

- 数据流分析
- 循环展开
- 常量传播
- 死代码消除
- 元编程代码生成优化

#### 8.4.2 LLVM 和 MLIR 支持

- 实现 LLVM 后端，利用其成熟的优化和代码生成能力
- 集成 MLIR 作为中间表示层，支持高级优化和异构计算
- 提供 MLIR 方言定义和转换框架，便于特定领域优化

#### 8.4.3 自定义汇编后端

- 开发专用汇编代码生成器，支持基础汇编语言输出
- 提供汇编指令集抽象层，便于支持多种目标架构
- 实现汇编代码优化和布局算法，生成高效汇编代码

## 九、应用场景

- 系统编程：利用内存安全模型开发操作系统核心组件
- 高性能计算：通过向量化与并发优化提升计算效率
- 跨平台应用：实现一次编写多平台部署
- 嵌入式开发：适合资源受限环境的轻量级部署
- WebAssembly：构建高效的前端计算模块

## 十、总结

SCP 编程语言通过创新的类型系统设计、先进的内存管理模型及多范式支持体系，在保证执行效率的同时，为开发者提供现代化的编程体验。其独特的区域内存管理机制与分层并发架构，有效解决了传统系统编程语言在安全性与并发性方面的技术难题，使其成为适用于从嵌入式系统到云服务开发的通用型编程语言。
